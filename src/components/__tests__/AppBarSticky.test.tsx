import React from "react";
import ReactDOM from "react-dom";
import AppBar from "../AppBarSticky";
it("Renders", () => {
  const div = document.createElement("div");
  ReactDOM.render(<AppBar />, div);
});
